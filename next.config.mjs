/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "avators.githubusercontent.com",
      "lh3.googleusercontent.com",
      "pokeapi.co",
    ],
  },
};

export default nextConfig;
